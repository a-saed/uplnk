# Automatically convert workspace: protocol to version ranges when publishing
publish-branch=main
link-workspace-packages=true

# This tells pnpm to replace workspace:* with the actual version when publishing
# No additional config needed - pnpm does this automatically during publish
