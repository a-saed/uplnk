# @uplnk/types

Shared TypeScript types for uplnk (`UplnkOptions`, `UploadProgress`, `UploadError`, etc.).

## Install

```bash
pnpm add @uplnk/types
```

Use with `@uplnk/core` or in your own wrappers.
