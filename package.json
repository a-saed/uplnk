{
  "name": "uplnk-monorepo",
  "private": true,
  "packageManager": "pnpm@9.14.2",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "docs:dev": "pnpm --filter uplnk-docs run dev",
    "docs:build": "pnpm --filter uplnk-docs run build",
    "docs:preview": "pnpm --filter uplnk-docs run preview"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
